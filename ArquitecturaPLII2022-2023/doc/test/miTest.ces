/* CONSTANTES */
#constante VERDADERO 1;
#constante FALSO 0;
#constante MAX 10;


/* TIPOS COMPUESTOS */ 
tipo entero vector[3];
tipo entero vector1[MAX];


/* VARIABLES */
entero a, b;
entero x;
entero suma;
entero mayorDiez;
entero uno = 1;
entero ocho = 8, tres = 3;
vector v0;
vector v;
vector1 v1, v2;

/* FUNCIONES */

entero funSuma (entero x, entero y) {
	
	/*tipos*/
	tipo entero vectorFunSuma[15];
	
	/*variables locales*/
	entero i;
	vectorFunSuma v3;
	entero suma;
	
	/*sentencias*/
	suma = x + y;
	suma = funSuma(i,v3[2]);
	
	devuelve suma;
	
}

entero funResta (entero a, entero b){
	devuelve a;
}

vacio escribeEntero (entero x) {
	
	/*tipos*/
	tipo entero vectorFunSuma[15];
	
	/*variables*/
	entero nueve = 9;
	entero fS;
		
	/*sentencias*/
	fS = funSuma(x,nueve);
	escribeEnt (x);
	escribeEntero(9);
	
	devuelve;
}

vacio saluda () {
 	escribe ("Hola mundo");
}

vacio principal () {
	
	/*tipos*/
	tipo entero vectorPrincipal[20];
	
	/* Declaraciones */
	entero a, b;
	entero i;
	entero dia = 1;
	
	/* Asignaciones */
	v[i] = 1 + MAX;  
	b = v[a]; 
	b = v[a++];
	a = 2 * funSuma(2,2);
	suma = funSuma (a, b);
	mayorDiez = !(suma < 10);
	a +=2;
	
	
	/* Sentencias */	
	
	a++;
	
	escribeEntero(funResta(a,b));	

	si (mayorDiez == VERDADERO) { escribe ("mayor"); }
	sino {escribe ("menor");}
	
	si (a==0){
	 entero a;
	 escribe ("es");
	 escribe ("falso");
	} sino {
	 entero varSino = 66;
	 escribe ("es");
	 escribe ("verdadero");
	}
	
	si (a==b) escribe ("iguales");	
	
	alternativas (dia){
	 caso 1: {escribe("lunes");} corte;
	 caso 2: {
	 escribe("es ");
	 escribe("martes");
	 } corte;
	 pordefecto: {escribe ("no es lunes ni martes");} corte;
	}
	
	mientras (a<5){
	 a=a+1;
	 escribeEnt(a);
	}
	
	mientras (a<5) a=a+1;

devuelve;

}